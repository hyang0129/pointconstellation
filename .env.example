# Never commit the populated .env file. Export these values in the shell that
# runs the EmpireAI tools; the repository does not load secrets automatically.
POINTCONSTELLATION_JUPYTER_PASSWORD=
POINTCONSTELLATION_JUPYTER_URL=http://localhost:18882
POINTCONSTELLATION_JUPYTER_KERNEL=p311
