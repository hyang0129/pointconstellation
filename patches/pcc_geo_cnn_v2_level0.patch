diff --git a/src/utils/octree_coding.py b/src/utils/octree_coding.py
index 6e32791..0e62494 100644
--- a/src/utils/octree_coding.py
+++ b/src/utils/octree_coding.py
@@ -57,7 +57,7 @@ def partition_octree_rec(points, bbox_min, bbox_max, level):
     if len(points) == 0:
         return [points], None
     if level == 0:
-        return [points], None
+        return [points], []
     bbox_min = np.asarray(bbox_min)
     bbox_max = np.asarray(bbox_max)
     ret_points, binstr, bboxes = split_octree(points, bbox_min, bbox_max)
@@ -74,7 +74,7 @@ def partition_octree(points, bbox_min, bbox_max, level):
     if len(points) == 0:
         return [points], None
     if level == 0:
-        return [points], None
+        return [points], []
     bbox_min = np.asarray(bbox_min)
     np.testing.assert_array_equal(bbox_min, [0, 0, 0])
     bbox_max = np.asarray(bbox_max)
@@ -116,5 +116,7 @@ def partition_octree(points, bbox_min, bbox_max, level):
 
 def departition_octree(blocks, binstr_list, bbox_min, bbox_max, level):
+    if level == 0:
+        return blocks
     bbox_min = np.asarray(bbox_min)
     bbox_max = np.asarray(bbox_max)
 
diff --git a/src/compress_octree.py b/src/compress_octree.py
index d1501dd..5294317 100644
--- a/src/compress_octree.py
+++ b/src/compress_octree.py
@@ -115,6 +115,7 @@ def compress():
                 if decode_files:
                     pc_io.write_df(cur_dec_files[j], pc_io.pa_to_df(cur_data['blocks_full']))
                 with open(of + '.enc.metric.json', 'w') as f:
-                    json.dump(cur_data['metrics'], f, sort_keys=True, indent=4)
+                    serializable_metrics = {key: float(value) for key, value in cur_data['metrics'].items()}
+                    json.dump(serializable_metrics, f, sort_keys=True, indent=4)
                 if args.debug:
                     pc_io.write_df(of + '.enc.ply', pc_io.pa_to_df(cur_data['blocks_full']))
diff --git a/src/model_types.py b/src/model_types.py
index bf0308a..98ef76b 100644
--- a/src/model_types.py
+++ b/src/model_types.py
@@ -160,7 +160,11 @@ def select_best_per_opt_metric(binstr, x_hat_list, level, opt_metrics, points, r
         cur_blocks_depart = [departition_octree(x, binstr, bbox_min, bbox_max, level) for x in cur_x_hat_list]
         cur_blocks_full = [np.vstack(x) for x in cur_blocks_depart]
-        cur_metrics_full = [compute_metrics(points[:, :3], x, resolution - 1, p1_n=get_normals_if(points, with_normals),
-                                            t1=t1) for x in cur_blocks_full]
+        cur_metrics_full = [
+            compute_metrics(points[:, :3], x, resolution - 1,
+                            p1_n=get_normals_if(points, with_normals), t1=t1)
+            if len(x) else {metric_key: -np.inf}
+            for x in cur_blocks_full
+        ]
         cur_metrics = [x[metric_key] for x in cur_metrics_full]
         local_best_idx = np.argmax(cur_metrics)
         best_idx = indexes[local_best_idx]
